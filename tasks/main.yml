---
- name: Load distro-specific vars
  include_vars: "{{ ansible_os_family }}.yml"

- name: Install base packages
  package:
    name: "{{ item }}"
    state: present
  with_items:
    - curl
    - ntp
    - vim
    - wget

- include: sshd.yml
  tags: sshd
- include: ntp.yml
  tags: ntp
